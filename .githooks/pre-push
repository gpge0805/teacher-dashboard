#!/bin/sh
# Block push if potential secrets are detected.
powershell -NoProfile -ExecutionPolicy Bypass -File "scripts/scan_secrets.ps1" -Mode prepush
status=$?
if [ $status -ne 0 ]; then
  echo "Push blocked by .githooks/pre-push (secret scan failed)."
  exit $status
fi
exit 0
